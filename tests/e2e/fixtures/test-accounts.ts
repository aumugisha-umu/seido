/**
 * Re-export from shared fixtures location.
 * Kept for backwards compatibility — prefer importing from 'tests/fixtures/test-accounts'.
 */
export { TEST_ACCOUNTS, BASE_URLS, getBaseUrl, type TestAccount } from '../../fixtures/test-accounts'
